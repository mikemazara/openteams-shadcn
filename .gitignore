node_modules
.eslintcache

# Lockfiles — Bun resolves from package.json; not committed
package-lock.json
.DS_Store
*.log

# Generated site output (Storybook at /, registry JSON at /r) — built in CI
/public
/storybook-static
*storybook.log
